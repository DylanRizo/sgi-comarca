generator client {
  provider     = "prisma-client"
  output       = "../src/generated/prisma"
  moduleFormat = "esm"
}

datasource db {
  provider = "postgresql"
}

// FASE 2 intentionally defines no persistent models. Business entities begin in FASE 3.
